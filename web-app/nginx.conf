server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA: cualquier ruta desconocida la resuelve el router de Angular.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
